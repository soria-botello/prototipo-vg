version: 0.2

phases:
  install:
    rutime-versions:
      nodejs: 18
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build
artifacts: 
  base-directory: build/
  files:
    - '**/*' 
